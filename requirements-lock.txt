# 全量回归通过时的**确切版本**。
#
# requirements.txt 给的是下限（"至少要这么新"），装出来是哪一套版本要看那天
# PyPI 上有什么。这个文件记的是"**1477 项全绿的那一套**"——复现问题、
# 排查"我这边跑不过"的时候，先按这个装一遍，能立刻分清是代码的问题还是
# 版本的问题。
#
# 记录环境：Linux x86_64 / CPython 3.11.15 / 2026-09-11
# 结果：1477 passed, 1 skipped（跳过的那项需要外部数据）
#
# 注意：这不是跨平台锁文件。Windows 上装 PySide6/VTK 的轮子不同，
# 版本号一致即可，不必强求 hash 一致。

numpy==2.4.4
scipy==1.17.1
matplotlib==3.10.9
jsonschema==4.26.0
python-docx==1.2.0
pillow==12.2.0

# 桌面端
PySide6==6.11.2
pyvista==0.49.0
pyvistaqt==0.13.1
vtk==9.7.0

# 网页端
streamlit==1.63.0
plotly==7.0.0

# 节点局部实体子模型的网格生成
gmsh==4.15.2

# MCP Server
mcp==2.1.1

# 测试
pytest==9.1.1
