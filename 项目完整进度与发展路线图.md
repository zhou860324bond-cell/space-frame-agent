# 空间刚架智能计算 Agent：完整进度、能力边界与发展路线图

> 报告日期：2026-09-06  
> 项目阶段：工程 Alpha（可运行、可演示、已有较完整自动测试，但尚不宜替代正式工程复核）  
> 统计口径：以当前工作区代码、本次工具清点、全量测试和桌面自检为准；历史文档中的旧数字不作为当前结论。
>
> **历史快照提示（2026-09-07）：** 本文记录的是六周冲刺前的 Alpha 基线。
> 当前 Beta 0.1 状态、数量与限制请以 `README.md` 和
> `docs/BETA_0.1_CAPABILITY_MATRIX.md` 为准。

## 1. 执行摘要

项目已经从“自然语言生成一个规则刚架并做线弹性计算”的早期原型，发展为一个具备独立求解内核、桌面 CAE 界面、参数化建模、Agent 工具链、结果查询与绘图、模态/屈曲、二阶效应和初步材料非线性的专业刚架分析原型。

当前最准确的定位不是“简化版 Abaqus”，也不是“已经完成的工程软件”，而是：

> 一个面向梁—柱空间刚架的、可验证、可扩展、由 Agent 驱动的专用 CAE 平台原型。

本轮核验得到的关键事实：

- Agent 当前注册 **41 个确定性工具**，已经覆盖建模、集合、属性、支座、荷载、求解、查询、绘图、扫参、包络、模态、屈曲、Abaqus 对比和学习轨迹。
- 桌面端当前注册 **54 个命令**，并通过 Python、求解器、Qt、PyVista、VTK/OpenGL 自检。
- 当前收集 **949 项测试**；指定项目内临时目录复跑后，表现为 **948 项通过、1 项按环境跳过**，仅出现 VTK 对 NumPy 2.5 的上游弃用警告。
- Timoshenko 剪切变形、杆端刚域偏移、小应变二阶弹性 P-Δ、双线性轴向材料塑性、理想铰接、振型曲线恢复和变形节点视觉连续性已经进入代码并有测试覆盖。
- 建模流程已经调整为“先几何、后属性、再边界/分析步/荷载、最后求解”，不再要求先定截面再定坐标。
- 复杂不规则刚架已经可以通过任意节点/杆件、折线门架、多跨多层多榀、单榀拉伸等方式建立，但尚缺少成熟的几何约束、自动交点剖分和“物理杆件—分析单元”分层，因此离顺畅建立大型复杂模型仍有明显距离。
- 后处理已经修复多类根本性错误，但当前仍属于“梁线结果可视化”，不能把它描述为连续体有限元意义上的应力云图。
- 当前最大的短板已经不再是“有没有按钮”，而是分析模型编译、非线性理论、结果数据库、交互一致性、基准验证和 Agent 训练/评测闭环尚未系统化。

## 2. 成熟度判断

| 子系统 | 当前判断 | 说明 |
| --- | --- | --- |
| 线性空间刚架内核 | 较成熟 | 具备 3D 梁、释放、刚域、杆件荷载、多工况、平衡校核和奇异诊断，已有较多解析回归测试 |
| 参数化与不规则建模 | 可用但不顺滑 | 有规则/不规则生成器和任意节点杆件入口；缺特征树、尺寸约束、自动交点剖分和网格编译层 |
| 荷载与边界条件 | 基础可用 | 支持 Initial 支座、分析步给定位移、节点荷载和多类杆件荷载；幅值曲线与 Step 传播/失活已有；缺随动力、高级约束、单元生灭与携带状态的施工过程分析 |
| 几何非线性 | 初步实现 | 当前为迭代更新轴力的小应变 P-Δ，不是有限转动共旋或 Abaqus `NLGEOM` |
| 材料非线性 | 研究性实现 | 当前仅双线性轴向塑性；弯曲仍为弹性，没有纤维截面与分布塑性 |
| 模态与屈曲 | 基础可用 | 已能求解并恢复杆内模态曲线；仍需质量源、参与质量、正交性和跨求解器基准体系 |
| 后处理 | 基础可用、仍需重构 | 变形、内力、模态和色标错误已显著修复；缺统一 Result DB、路径/探针/包络工作流和结果语义元数据 |
| 桌面前端 | 工程原型 | 已有 CAE 模块化界面、模型树和视口；部分命令仍是独立对话框，状态管理与 Abaqus 式对象工作流未完全贯通 |
| Agent | 功能丰富、治理不足 | 工具覆盖面较广且坚持数值由内核产生；缺稳定领域 IR、能力矩阵、错误分类、置信度和系统化训练评测 |
| 多模态输入 | 有入口、未形成可靠产品能力 | 已有图片上传和草图解析链路；缺标注确认、尺度校准、拓扑消歧和批量评测 |
| 工程化与发布 | 偏弱 | 有大量测试和启动自检，但文档数字陈旧、当前目录未检测到 Git 仓库，CI、版本基线和发布治理仍需补齐 |

## 3. 已经实现的功能

### 3.1 建模与几何

- 支持显式添加、编辑节点和杆件，不规则模型不必依赖规则生成器。
- 支持规则多层、多跨、多榀空间刚架生成。
- 支持不等跨、不同层高、门式刚架、单榀生成后沿空间方向拉伸。
- 支持用任意 `x:z` 折线和各柱柱脚坐标描述不规则平面门架。
- 支持斜撑、节点抬升、杆件改接、删杆和重构。
- 重复坐标可以自动吸附；重合但没有建立拓扑连接的节点会在求解前被检查。
- 已把默认流程改成几何优先，参数化生成默认只生成几何，不强行混入截面、材料、柱脚和荷载。
- 支持命名集合，用于后续区域选择与批量操作。

### 3.2 材料、截面与构件属性

- 各向同性线弹性材料。
- 材料密度，可用于模态质量矩阵。
- 双线性轴向材料参数和增量非线性求解。
- 多种常用截面类型及截面属性计算。
- 截面可选 `Ay/Az` 有效剪切面积；填写后启用 Timoshenko 剪切变形，不填写时保留 Euler–Bernoulli 分支。
- 材料和截面可以在几何完成后分配，不再成为创建节点坐标的前置条件。
- 支持杆件参考方向/局部轴控制。

### 3.3 连接、释放和刚域

- 支持杆端任意局部自由度释放，并通过静力凝聚进入刚度和结果恢复。
- 前端提供显式“创建铰接”入口，可按构件端部建立理想铰。
- 支持 `offset_i/offset_j` 全局向量刚域偏移。
- 刚域已进入刚度、质量、几何刚度、荷载等效和内力恢复链路，而不只是显示层装饰。
- 后处理会单独显示节点至梁端的刚臂，避免把偏移后的分析端点误认为节点脱开。

### 3.4 边界条件与荷载

- 支持六自由度逐项约束和常用固定/铰支预设。
- Initial 支座与分析步中的给定位移已经分开。
- 支持节点力和节点力矩。
- 支持杆件均布荷载、梯形/线性变化荷载、杆中集中荷载和跨段荷载。
- 支持自重。
- 同一节点或杆件上的多条命名荷载会物理叠加，可按名称编辑和删除，避免字典覆盖造成静默丢载。
- 支持线性工况和荷载组合、结果扫参与包络查询。

### 3.5 求解内核

- 三维空间梁单元，每节点 6 个自由度、每单元 12 个自由度。
- Euler–Bernoulli 与 Timoshenko 两种剪切处理分支。
- 局部—全局坐标变换、杆端释放、刚域偏移和一致荷载处理。
- 线性静力、多工况共用装配/分解、线性组合。
- 静力平衡检查、刚度奇异诊断和低能模态定位。
- 刚度矩阵自由自由度子块支持对称 Jacobi 对角缩放，降低尺度和刚度跨数量级时的误判。
- 小应变二阶弹性 P-Δ：逐级加载、根据当前轴力更新几何刚度并迭代收敛。
- 双线性轴向材料非线性：增量加载、返回映射、切线更新和残差/迭代记录。
- 线性模态分析和线性特征屈曲分析。
- Abaqus 输入/运行/结果读取与本地求解器对比入口；实际运行依赖本机 Abaqus 环境。

### 3.6 后处理

- 梁中心线变形图不再只连接两个变形节点，而是由梁端平移和转角恢复单元内部曲线。
- 单元释放端会先恢复真实端转角，再插值得到杆内曲线。
- 振型和失稳模态同样使用梁曲线恢复，不再仅画两节点折线。
- 模态自动缩放只使用恢复后的中心线平移，不混用长度与转角量纲。
- 变形图使用共享节点球和独立刚臂线，降低复杂模型中的视觉断缝。
- 支持轴力、剪力、扭矩、两个方向弯矩和合弯矩图。
- 空间默认云图使用局部轴旋转不敏感的合弯矩 `|M|`；有符号局部分量使用关于零对称的发散色标。
- 集中力处的剪力跳跃不会再被简单等距重采样抹平。
- 公共节点处不再把相邻杆件的杆端标量错误平均。
- N-mm-MPa 等单位制下的坐标、位移和内力显示换算已统一走单位模块。
- 支持数值查询、构件图查询、图像导出、Markdown/Word 报告和实验胶囊记录。

### 3.7 Agent 与可验证闭环

当前 41 个 Agent 工具按能力可分为：

- 几何生成与编辑：节点、杆件、规则刚架、门架、多榀拉伸、支撑、变截面、斜撑。
- 属性与集合：材料、截面、属性分配、命名集合和选择。
- 荷载与边界：工况、节点荷载、杆件荷载、跨段荷载、给定位移和自重。
- 分析：模型校验、线性/P-Δ/材料非线性求解、模态、屈曲和 Abaqus 对比。
- 结果：查询、图形、构件图、扫参、包络和报告。
- 学习：导出包含工具实参、工具结果、模型变化、最终模型和验证标志的学习轨迹。

系统的正确原则已经建立：大模型负责意图理解和结构化决策，位移、内力、反力、频率等数值必须来自确定性求解工具。只有 `verified=true` 的轨迹才适合作为正确训练样本；失败轨迹可以用于学习诊断与修复过程。

## 4. 用户此前明确提出功能的落实状态

| 需求 | 状态 | 当前真实边界 |
| --- | --- | --- |
| Timoshenko 剪切变形 | 已实现 | 通过 `Ay/Az` 开启；仍需更多深梁、不同剪切系数和跨求解器基准 |
| 刚域偏移 | 已实现 | 已进入核心矩阵、荷载和结果恢复；复杂节点域与面板区尚未实现 |
| P-Δ | 已实现基础版 | 是小应变二阶弹性迭代，不等于有限转动、随动力和完整 `NLGEOM` |
| 材料非线性 | 部分实现 | 只有轴向双线性塑性；弯曲塑性、纤维截面、循环退化尚未实现 |
| 铰接点 | 已实现基础版 | 当前主要是理想杆端释放；半刚连接、弹簧铰、塑性铰尚未实现 |
| 修复变形图节点分离 | 已修复主要链路 | 共享节点与刚臂显示已处理；大规模混合释放/偏移模型仍需专门视觉基准集 |
| 修复模态图 | 已完成第一轮 | 已恢复杆内曲线和正确缩放；质量源、参与质量、MAC 等仍缺 |
| Abaqus 式建模流程 | 部分落地 | Geometry → Property → Initial BC → Step/Load → Solve 已形成；对象树、Step Manager、Job Monitor、输出请求仍不完整 |
| 参数化建模 | 基础可用 | 当前偏“参数生成器”；还不是带依赖关系、可回滚重生成的特征树系统 |
| 多模态输入 | 原型入口 | 图片→解析→模型链路存在，但可靠确认、尺度标定、拓扑消歧和评测不足 |
| Agent 学习自己的建模求解过程 | 数据链路已起步 | 有可验证轨迹；尚无成熟数据治理、课程学习、回归评测和模型版本比较 |

## 5. 本次验证证据

### 5.1 自动测试

- 当前测试收集数：949。
- 复跑结果：948 通过，1 个按环境条件跳过。
- 唯一成批警告来自 VTK 的 NumPy 2.5 弃用接口，不是本项目断言失败。
- 初次使用系统默认临时目录运行时出现大量 `WinError 5`，错误统一发生在 pytest 创建临时目录的阶段；改用项目内 `--basetemp` 后全量通过。这说明代码没有对应回归，但测试启动脚本还应自动处理不可写系统临时目录。

### 5.2 桌面自检

- Python 3.12.14：通过。
- NumPy / SciPy / jsonschema：通过。
- 内核单跨单层框架求解：通过。
- PySide6 6.11.2 与 qtpy 绑定：通过。
- PyVista 0.48.4、VTK 9.6.2：通过。
- OpenGL 渲染：通过。

### 5.3 已有验证层次

- 解析解回归：悬臂挠度、扭转、简支均布荷载、超静定反力等。
- Abaqus 对标算例：B33/Euler–Bernoulli 与 B31/Timoshenko 路径。
- Agent 离线评测与状态评分。
- 静默失败检查：约束不足、位移异常、反力/内力异常、近平衡失败和截面方向风险。
- 实验胶囊：保存输入、结果摘要、环境和哈希，便于复现与比较。

必须强调：测试通过证明“已覆盖行为没有发生已知回归”，不等于已经达到工程认证、规范验收或商业有限元软件的验证规模。

## 6. 当前最重要的不足

### 6.1 建模层：物理对象与分析离散没有分层

当前一根用户看到的杆件通常也是一个分析单元。对于以下情况，这种模型会迅速变得不稳健：

- 杆中集中荷载或内节点。
- 相交杆件的自动连接。
- 局部截面、材料或方向变化。
- 塑性积分点和分布塑性。
- 局部刚域、铰、弹簧和构件端区。
- 后处理需要保持一根物理构件连续，而分析又必须自动剖分。

需要引入 **Physical Member（物理构件）→ Analysis Element（分析单元）** 编译层。用户操作物理构件，求解器按交点、集中荷载、释放、属性突变和非线性积分需求自动剖分，结果再聚合回物理构件。

### 6.2 参数化建模仍是生成器，不是特征树

现有参数化能力能快速生成模型，但修改上游参数后，集合、荷载、截面分配和结果引用缺少稳定的拓扑命名与依赖更新。真正成熟的参数化系统至少需要：

- 参数与表达式。
- 草图尺寸和几何约束。
- 特征依赖图与局部重生成。
- 稳定对象 ID 和拓扑命名。
- 重生成前差异预览。
- 失败特征定位与回滚。

### 6.3 非线性内核尚未形成统一框架

- P-Δ 与材料非线性是两条独立能力，尚未形成几何—材料联合非线性。
- 没有共旋梁柱或有限转动运动学。
- 没有纤维截面，无法正确表达轴力—双向弯矩耦合屈服。
- 没有力法梁柱、分布塑性或集中塑性铰积分方案。
- 没有卸载、反向加载、循环滞回、刚度/强度退化。
- 没有线搜索、自动减小增量、位移控制和弧长法。
- 没有能量误差与更系统的收敛诊断。

因此，当前“材料非线性”不能用于宣称框架抗震弹塑性分析已经完成。

### 6.4 连接和约束能力不足

当前理想铰和固定约束可用，但复杂刚架常需要：

- ~~转动弹簧与半刚性连接~~ 已实现（`Member.springs_i/j` +
  `set_member_connection`）：静力凝聚里把 `K_rr` 换成 `K_rr + k_s`，理想铰
  是 k=0 的极限、刚接是 k=∞ 的极限，不另写一套单元。**六个自由度一视同仁**
  ——凝聚不区分转动与平动，所以轴向、剪切方向的连接弹簧同样可用，
  各自对着闭合解验过（转动 δ = PL³/3EI + PL²/k，轴向 δ = PL/EA + P/k，
  都跨五个数量级）。**还缺**：连接的非线性骨架曲线（现在是线弹性弹簧，
  不会屈服也不会退化）、连接自由度之间的耦合。
- 塑性铰骨架曲线。
- 只拉/只压构件、间隙与接触型连接。
- 刚性楼板、EqualDOF、多点约束和刚体。
- 梁柱节点面板区、剪切变形和连接偏心。
- 初始缺陷、残余应力和施工阶段。

### 6.5 动力分析仍停留在模态入口

- 质量来源和附加节点质量管理不完整。
- ~~模态参与因子与有效质量~~ 已实现（`ModalResult.participation` /
  `effective_mass` / `participable_mass`）。**参与质量比的分母是能参与振动的
  质量 `rᵀM_ff r`，不是 Σ ρAL**：压在支座上的质量永远不参与，拿 Σ ρAL 当
  分母的话，一根剖成 4 段的悬臂柱把振型取满也只报得出 84%，而"加大阶数"
  这条建议在那里是无效的。取满振型时新口径恰好 100%，与网格无关。
  **还缺**：正交性与 MAC 检查、附加节点质量。
- ~~反应谱与 SRSS/CQC 组合~~ 已实现（`src/spectrum.py` +
  `response_spectrum_analysis`），含 GB 50011 设计谱。对着"常量谱下各阶基底
  剪力之和 = 可参与质量 × Sa"这条恒等式验过，相对差 0。
  **还缺**：双向地震的方向组合、偶然偏心、竖向地震的专门规定。
- 缺少 Newmark、HHT 或 generalized-α 时程积分。
- 缺少阻尼模型、地震动输入、时程曲线、滞回曲线和能量图。

### 6.6 后处理缺统一结果数据库

当前结果主要围绕一次 `Solution` 和若干数组/绘图函数组织。随着非线性、动力和多步分析增加，会遇到结果语义不清的问题。建议建立类似 Abaqus 但更轻量的结构：

```text
Analysis
└── Step
    └── Increment / Frame
        ├── NodalOutput: U, UR, RF, RM
        ├── ElementOutput: N, Vy, Vz, T, My, Mz
        ├── SectionOutput: strain, stress, plastic_state
        └── Diagnostics: residual, iterations, energies, warnings
```

每个输出都应携带单位、坐标系、位置、分量定义、平均/外推规则、Step、增量、时间和收敛状态。没有这些元数据，图画得再漂亮也容易误读。

### 6.7 “云图”的概念需要收紧

梁单元结果沿中心线或截面恢复，本质上不是实体/壳单元的连续应力场。当前 UI 应把名称明确为“杆件结果着色”或“梁线云图”，并在图例中说明：

- 是杆件内力、截面应力还是节点量。
- 局部轴方向和正负号约定。
- 结果位置是端点、积分点还是插值点。
- 公共节点是否平均；不连续量默认不应平均。
- 极值是物理值还是显示插值值。

### 6.8 前端对象工作流尚未完全贯通

- Step 已经是一等对象（`src/steps.py` + `add_step` / `list_steps` / `delete_step`
  / `solve_steps`）：荷载与边界条件**跨 Step 传播**，可在后续 Step 里改写或
  失活，语义与 Abaqus 一致。幅值曲线同样落地（`define_amplitude` +
  `solve_model(analysis="step")`）。
  还缺的是：**每一步都从未变形、无应力状态重解**，不携带上一步的状态；
  杆件不能在步之间生灭（没有 birth/death）。所以它表达的是「同一结构的
  几种配置」，不是施工过程——后者要先有能携带状态的增量求解器。
  桌面端的「分析步」按钮也还是求解选项对话框，没接到这套 Step 上。
- 材料非线性还进不了分析步：一个 Step 里常有几个工况同时生效，而材料
  非线性不能事后叠加，缺一个「把几个工况合成一个再增量求解」的入口
  （P-Δ 那边是 `solve_step`，材料那边没有）。
- 缺少独立 Job 对象、后台进度、日志、终止、重启和失败诊断面板。
- Output Request 尚未成为一等对象。
- 模型树的状态、区域选择、抑制和依赖关系还不够强。
- 高级参数仍散落在多个对话框，缺少“基本/高级”分层和一致的提交/取消习惯。
- 选择过滤器、工作平面、吸附、框选、路径选择和局部坐标可视化仍需增强。
- 大模型下的交互性能和撤销粒度尚需专项测试。

### 6.9 多模态输入缺少“人机确认层”

图片识别不能直接成为求解模型。可靠流程应为：

1. 识别几何线、节点、尺寸、支座、荷载和文字。
2. 输出带来源框和置信度的结构化草稿。
3. 对缺失尺度、遮挡、交叉但不连接等问题主动询问。
4. 在视口叠加识别结果，让人点击确认/修正。
5. 通过拓扑、单位和物理规则校验后再编译为分析模型。
6. 保存原图、识别结果、人工修订和最终模型之间的可追溯关系。

当前主要只有第 1、2、5 步的原型链路，其余是正式落地的关键。

### 6.10 Agent 的“学习”还不是成熟训练系统

已有轨迹导出是正确起点，但仍缺：

- 统一、版本化、强类型的领域中间表示（IR）。
- 每个工具的前置条件、后置条件、能力边界和失败分类。
- 样本去重、难度分级、泄漏检查和错误标签。
- 解析解、跨求解器和人工审查共同构成的样本验收。
- 训练前后固定评测集、对抗案例和负例。
- 模型版本、提示版本、工具版本和内核版本的联合追踪。
- 对“正确建模过程”而不仅是“最终答案”的评分。

### 6.11 工程化基础存在明显欠账

- 当前工作目录未检测到 Git 仓库，缺少正式提交历史、分支、标签和可靠回滚基线。
- README 仍保留“710 个测试”“8 个工具”等旧信息，与当前 949 项测试和 41 个工具不一致。
- 历史总结同时包含新旧统计，读者容易误判。应由代码自动生成能力清单和测试徽标。
- pytest 默认系统临时目录在当前环境可能不可写；测试入口应自动降级到项目内临时目录。
- 根目录密钥文件虽然文档称已忽略，但仍建议迁移到环境变量/系统凭据存储，并通过版本库扫描确认从未提交。
- 当前验证主要集中在 Windows 和单一依赖组合，缺少 CI 矩阵、版本锁定和可重复发布包。
- `main_window.py`、Agent 会话和部分结果逻辑仍偏集中，继续叠加功能会放大耦合和回归风险。

## 7. GitHub 相似项目可借鉴内容

以下建议以借鉴架构和验证方法为主，不建议直接复制受许可证约束的代码。

| 项目 | 值得吸收的能力 | 对本项目的具体启示 |
| --- | --- | --- |
| [PyNite](https://github.com/JWock82/Pynite) | 3D 梁架、P-Delta、模态、PyVista、物理构件自动剖分、组合与包络 | 优先实现 Physical Member；保持建模对象连续、分析单元自动离散 |
| [Frame3DD](https://github.com/pslack/frame3dd) | 成熟的静力/动力刚架求解、模态参与、缩聚、错误诊断 | 建立小而硬的权威基准集；强化动力结果和矩阵诊断 |
| [anaStruct](https://github.com/ritchie46/anaStruct) | 简洁 API、铰/弹簧、非线性节点、易用绘图 | 面向日常工程任务设计更短的建模路径和连接器 API |
| [OpenSees](https://github.com/OpenSees/OpenSees) | 纤维截面、力法梁柱、塑性铰积分、共旋变换、丰富求解算法 | 非线性升级应围绕“材料—截面—单元—算法”四层接口，不应继续在单一线弹性梁函数上打补丁 |
| [sectionproperties](https://github.com/robbievanleeuwen/section-properties) | 任意截面有限元属性、翘曲和应力恢复 | 把截面分析做成独立可插拔子系统，为纤维截面和任意截面奠基 |
| [PrePoMax](https://github.com/ISCPC/PrePoMax) | CaeModel/CaeResults/CaeJob/UI 分层、命令与 Regenerate 工作流 | 用命令对象统一 GUI 和 Agent 修改；任何模型变更都可回放、撤销和重生成 |
| [StructureClaw](https://github.com/structureclaw/structureclaw) | Draft → Validate → Analyze → Check → Report；多引擎合同和可追溯 Agent | 把 Agent 流程固化为状态机，增加能力策略、置信度、证据和失败恢复 |
| [AutoBM](https://github.com/Jovanqing/AutoBM) | 可执行建模数据集、分层物理评测、周期/位移/承载力指标 | 学习系统必须有独立基准集和物理一致性指标，不能只看工具调用成功率 |
| [OASiS](https://github.com/Hereon-InstituteMS/OASiS) | 多求解器 Agent、解析验证、网格收敛和统一接口 | 建立本地/OpenSees/CalculiX/Abaqus 的统一后端和结果合同 |
| [Kratos](https://github.com/KratosMultiphysics/Kratos) | 插件式多物理架构、非线性和动力模块 | 借鉴注册表与应用插件思想，但当前不要扩张到通用多物理大平台 |

补充参考：OpenSees 的 [Force-Based Beam-Column Element 文档](https://opensees.github.io/OpenSeesDocumentation/user/manual/model/elements/forceBeamColumn.html) 对分布塑性、截面积分和坐标变换的接口关系很有借鉴价值；Frame3DD 的 [用户手册](https://github.com/pslack/frame3dd/blob/master/doc/Frame3DD-manual.html) 适合作为线性静力/动力功能和验证条目的检查表。

## 8. 推荐的目标架构

```text
自然语言 / 表格 / 图片 / 手工操作
                │
                ▼
        Versioned Domain IR
        ├─ Geometry / Parameters
        ├─ Materials / Sections
        ├─ Physical Members / Connections
        ├─ Sets / BC / Loads / Steps
        └─ Output Requests
                │
        Validate + Diff + User Confirm
                │
                ▼
       Analysis Model Compiler
       ├─ topology resolution
       ├─ automatic segmentation
       ├─ DOF / MPC generation
       ├─ load conversion
       └─ solver capability check
                │
                ▼
          Solver Adapter API
       ├─ Native linear/nonlinear
       ├─ OpenSees
       ├─ CalculiX
       └─ Abaqus
                │
                ▼
          Unified Result DB
       ├─ steps / frames / fields
       ├─ units / coordinates / locations
       ├─ convergence / energies
       └─ provenance / verification
                │
                ▼
     Visualization / Check / Report / Learning
```

这套架构的核心价值是：界面、Agent 和脚本都修改同一种领域模型；各求解器只接收编译后的分析模型；所有结果进入统一结果数据库。这样才能避免 GUI 一套逻辑、Agent 一套逻辑、Abaqus 导出又一套逻辑。

## 9. 分阶段路线图

### P0：可靠性和项目治理基线（最高优先级）

交付内容：

- 建立 Git 仓库、首个可复现基线标签和变更日志。
- 固定依赖版本，建立 Windows CI；后续增加至少一个无 GUI 的第二环境。
- 修复测试入口的临时目录降级，确保一条命令稳定得到 949 项测试结果。
- 从工具注册表和测试收集结果自动生成 README 能力表，删除陈旧数字。
- 建立 `Capability Matrix`，明确每个功能的状态、理论、限制、测试和对标对象。
- 将密钥迁移到环境变量或系统凭据，并做仓库历史密钥扫描。
- 拆分过大的窗口/会话模块，建立模块依赖约束。

验收标准：

- 新环境按文档一次启动成功。
- 全量测试不依赖系统临时目录权限。
- README、程序 About 和能力矩阵数字一致。
- 每次结果文件都能记录代码版本、模型哈希和依赖版本。

### P1：物理构件、分析编译与 Result DB

交付内容：

- Physical Member 与 Analysis Element 分层。
- 交点、内节点、集中荷载、释放、偏移、属性变化自动剖分。
- 稳定对象 ID、集合映射和结果回聚。
- Step/Frame/FieldOutput 结果数据库。
- 输出请求、路径查询、探针、包络和连续物理构件图。
- GUI 中提供分析网格显示开关，但默认让用户操作物理构件。

验收标准：

- 一根跨中有集中荷载的梁，用户仍只管理一根物理构件，求解器自动生成必要单元。
- 修改构件长度后，原有属性、集合和荷载在规则允许时保持引用。
- 所有图和表从 Result DB 读取，不再各自重复推导结果语义。

### P2：真正的梁柱非线性平台

交付内容：

- 一维材料统一接口与状态变量。
- 纤维截面几何、材料分区、轴力—双向弯矩积分和切线。
- 共旋梁柱运动学。
- 位移法或力法非线性梁柱；集中塑性与分布塑性至少各一条路径。
- 几何—材料联合非线性。
- 自动增量、减小步长、线搜索、位移控制，随后再考虑弧长法。
- 弹簧、半刚连接、只拉/只压和初始缺陷。

验收标准：

- 截面单调弯矩—曲率与独立纤维计算一致。
- 悬臂塑性铰、门架推覆和柱屈曲后路径与 OpenSees/Abaqus 基准吻合在预设容差内。
- 每个增量可查询残差、迭代、能量、材料状态和失败原因。

### P3：动力与工程判断能力

交付内容：

- 质量源、附加质量、模态参与质量、正交性和 MAC。
- 阻尼、反应谱、SRSS/CQC。
- Newmark/HHT/generalized-α 时程分析和地震动输入。
- 时程、相图、滞回和能量后处理。
- 规范验算采用插件方式接入，不把规范逻辑写死在求解内核。

验收标准：

- 典型剪切型框架频率、振型、参与质量和反应谱结果通过解析与外部求解器对标。
- 时程积分通过自由振动、受迫振动和能量衰减基准。

### P4：多模态 Agent 与学习闭环

交付内容：

- 图片、表格和自然语言统一映射到版本化 Domain IR。
- 图片识别叠加层、尺度标定、拓扑消歧和逐项确认。
- Agent 固定状态机：Draft → Validate → Confirm → Analyze → Verify → Report。
- 干运行和模型差异预览，重要修改确认后提交。
- 能力策略：求解器不支持时必须拒绝、降级或切换后端，不得静默近似。
- 轨迹数据治理、课程难度、负例、对抗集、物理一致性评分和版本回归。
- 本地/OpenSees/CalculiX/Abaqus 多引擎验证合同。

验收标准：

- 多模态识别不完整时不会直接求解，而会定位缺失信息并请求确认。
- 固定评测集同时考核最终模型、过程正确性、物理结果和解释可追溯性。
- Agent 生成的任何数值都能回溯到工具结果和 Result DB 字段。

### P5：面向正式使用的产品化

交付内容：

- 项目格式迁移、向后兼容和崩溃恢复。
- 大模型性能、内存和交互基准。
- 插件 SDK、扩展注册表和版本兼容策略。
- 安装包、自动更新、诊断包和匿名错误报告选项。
- 用户手册、验证手册、理论手册和示例库。

验收标准：

- 版本升级不会静默改变模型语义或结果定义。
- 关键错误均能生成可分享、无密钥的诊断包。
- 发布版本具有完整的验证矩阵和已知限制清单。

## 10. 下一迭代建议清单

建议下一轮只集中完成以下十项，不再横向增加新单元类型：

1. 初始化 Git 与可复现版本基线。
2. 修正测试启动脚本的 `--basetemp` 策略，清理 README 旧统计。
3. 定义版本化 Domain IR 和 Capability Matrix。
4. 设计 Physical Member / Analysis Element 数据结构和映射测试。
5. 实现集中荷载、交点、释放和属性突变的自动剖分。
6. 建立 Result DB 最小版本，让线性静力和模态先迁入。
7. 把 Step、Output Request 和 Job 做成一等对象，贯通模型树与命令栈。
8. 建立 20～30 个“小而硬”的金标准算例，覆盖释放、刚域、Timoshenko、P-Δ、模态和屈曲。
9. 设计一维材料与纤维截面接口，先做截面级单元测试，不急着接 GUI。
10. 为多模态加入识别叠加、尺度确认和拓扑确认，不再允许图片识别结果直接成为可信分析模型。

## 11. 我认为最值得发展的差异化方向

### 11.1 “可解释建模编译器”而不是聊天壳

Agent 每次修改模型都生成结构化 diff：新增了什么、删了什么、单位是什么、为何选择该分析步、哪些参数是默认值、哪些结论尚未验证。用户可以像审查代码一样审查模型变更。

### 11.2 “求解器副驾驶”而不是只会提交作业

当模型奇异、不收敛或结果异常时，系统应给出：最可能的自由度、相关构件、证据、可逆修复建议和修复后的差异预览，而不是只显示矩阵奇异或迭代失败。

### 11.3 结果置信卡

每次分析生成一张置信卡：理论假设、单元类型、网格/剖分、收敛状态、平衡误差、适用范围、对标情况、未验证功能和风险等级。它比单纯显示“求解成功”更符合工程习惯。

### 11.4 基准算例画廊

把每个新功能绑定到一个可视化基准：模型、解析式、外部求解器结果、误差、版本趋势和失败案例。用户既能学习，也能看见软件为什么可信。

### 11.5 场景分支与参数实验

允许从同一模型分支出不同截面、支座、荷载和分析设置，比较位移、内力、质量、频率和安全指标；Agent 负责生成实验矩阵，但数值仍由求解器提供。

### 11.6 混合求解后端

本地内核负责快速线性分析和交互预览，OpenSees/CalculiX/Abaqus 负责超出本地能力边界的验证或高级求解。统一结果合同使切换后端不改变用户工作流。

## 12. 现阶段不建议做的事情

- 暂不扩展壳、实体、接触、热分析和通用多物理场；这会稀释刚架主线并显著放大验证成本。
- 暂不宣称达到 Abaqus/OpenSees 等成熟软件的非线性能力。
- 不让大模型直接生成并执行未经类型检查的底层求解器代码。
- 不把物理构件与分析单元继续混为一谈。
- 不在主界面一次性暴露全部高级参数，应使用合理默认值、模板和高级折叠区。
- 不直接复制 GPL 等项目代码到闭源或许可证不兼容的产品中；优先借鉴算法论文、数据合同和测试思想。
- 不以“测试数量很多”代替独立基准、网格收敛、跨求解器对比和工程审查。

## 13. 风险清单

| 风险 | 严重度 | 处置建议 |
| --- | --- | --- |
| 非线性能力被 UI 名称夸大 | 高 | 在界面和报告中明确“轴向双线性”“小应变 P-Δ” |
| 物理构件与分析单元混用导致复杂模型不可维护 | 高 | 下一阶段优先完成分析模型编译层 |
| 后处理字段缺少位置、坐标系和平均规则 | 高 | 以 Result DB 为唯一结果来源 |
| 文档与代码状态不一致 | 高 | 自动生成能力清单与测试统计 |
| 没有版本库基线 | 高 | 立即建立 Git、标签和变更记录 |
| 图片识别错误直接进入求解 | 高 | 强制草稿、叠加确认和拓扑校验 |
| 复杂模型只靠单元测试、缺系统基准 | 高 | 建立金标准模型库和跨求解器容差 |
| 大型窗口和 Agent 会话继续膨胀 | 中 | 按领域服务、命令、视图模型和适配器拆分 |
| VTK/NumPy 上游弃用 | 中 | 锁定兼容版本并跟踪 PyVista/VTK 更新 |
| 高级功能扩张过快 | 中 | 用 Capability Matrix 和阶段门限制未验证功能进入“正式”状态 |

## 14. 下一里程碑的完成定义

建议把下一个正式里程碑定义为：

> 用户可像 CAE 软件一样创建和修改物理构件；系统能自动生成分析离散；线性静力、P-Δ 和模态结果统一进入 Result DB；所有模型修改可追踪、可撤销、可重生成；复杂节点、集中荷载和释放不会迫使用户手工拆杆。

该里程碑完成时至少满足：

- 复杂不规则模型的创建不依赖直接编辑完整 JSON。
- 一根物理构件允许自动包含多个分析单元。
- 模型树能区分几何、属性、分析步、荷载、作业和结果。
- 求解前能显示模型差异、能力边界和自动剖分结果。
- 线性/P-Δ/模态后处理全部读取统一结果字段。
- 关键算例同时通过解析、本地回归和至少一种外部求解器对标。
- 文档、程序显示和测试统计完全一致。

## 15. 最终判断

现在的项目已经有了“产品骨架”和“自主内核”，并不是只能演示的空壳。近期对启动、Qt、建模顺序、复杂几何入口、荷载覆盖、Timoshenko、刚域、P-Δ、轴向塑性、铰接、模态曲线和后处理连续性的修复，解决了多项会直接误导用户或阻断使用的问题。

但它仍处于工程 Alpha：能做可信的、受能力边界约束的梁柱刚架分析；不能把当前的非线性、多模态和“云图”能力外推成通用有限元平台能力。下一步最重要的不是继续堆按钮，而是完成 **物理构件—分析模型编译、统一 Result DB、真正的非线性梁柱框架、验证体系和 Agent 数据治理**。

如果坚持“专注刚架、数值可验证、过程可追踪、Agent 不编造结果”这条路线，这个项目最有机会形成的独特优势，不是复制 Abaqus 的全部功能，而是让复杂刚架建模、分析、诊断、对比和学习过程比传统 CAE 更透明、更易复用，也更适合人与 Agent 协同。
