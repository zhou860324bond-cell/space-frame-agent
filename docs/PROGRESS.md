# 工作进度

Codex 与 Claude Code 共用的交接记录。开工先读，收工更新；最新的写在最上面。

## 当前状态（2026-09-25）

- **master**：已合入 PR #19（前端抽屉化、流畅度）、#20（应力比图、节点实体自动放粗网格）。
- **PR #21 `claude/layout-and-polish`**：CI 三个平台全绿，**等用户确认后合并**。内容：
  - 布局：边界条件移到左侧；右侧只留「AI 助手」；草图识别改独立窗口；抽屉头去掉页签。
  - 自动保存（改动后 1.5 s，异常退出后启动时询问恢复）；记住窗口、抽屉尺寸与云图选项。
  - 节点实体开算前选 1 档 / 3 档，逐档显示进度。
  - SuperLU 对称模式提速约 30%；并列峰值取第一个（修单位不变性测试）。
  - 真渲染冒烟 `tools/render_smoke.py`（32 项检查）。
- 回归：2233 通过、1 项按环境跳过（本机装了 Abaqus）。

## 下一步

1. 用户确认后合并 PR #21。
2. **中期答辩（约 2026-10-09，五分钟）**：
   - 答辩前一周出幻灯片完整初稿，前 3 天定稿；冻结演示版本并打 tag，录演示视频兜底。
   - 演示脚本：一句话建模 → 预演确认 → 求解 → 云图 / 内力图 / 应力比 → 出报告。节点实体不现场跑（3 档要 352 s）。
3. `docs/课程报告.md` 7.2 节数字自相矛盾：一处写「通过率从 93% 降到 85.7%」（09-16 旧数），5.4 节是 99.1%（09-23 新数）。统一成新数，旧数标明是历史数据。
4. 报告第 8 节分工表还是「（待填）」，需小组成员填写。
5. 使用手册截图还是旧布局，按新布局重截。

## 已知问题

- **节点实体给不出 Kt**：用户模型的 6 杆节点（D219×8，节点 14）3 档网格（94.0 / 62.7 / 41.8 mm）峰值应力 290 / 245 / 219 MPa，热点应力收敛判断为「不确定」，按规则不给 Kt。更细的网格超出自研求解器规模（8 万单元 / 18 万自由度），需用 Abaqus。
- **Esc 取消拾取、关闭抽屉**只在无头测试里验证过，还没在真窗口里手动确认。
- `desktop/main_window.py` 已近 2,900 行，职责偏多。
