# 工作进度

Codex 与 Claude Code 共用的交接记录。开工先读，收工更新；最新的写在最上面。

## 当前状态（2026-09-26）

- **master**：已合入 PR #19、#20、**#21**（布局重排、自动保存、记住设置、节点实体分档、SuperLU 提速、真渲染冒烟、AGENTS.md 与本文件）。
- **`claude/collect-codex-pass`**：收 Codex 在 `codex/optimization-pass` 上的一轮（f07e7b6），并补两处：
  - Codex 那轮：修复零参与质量方向的模态质量比；wheel 补齐平铺模块与 Pillow 依赖；主窗口拆出命令、结果、设置 mixin；修复整窗截图覆盖层合成并按新布局重拍五张手册图；校准报告第 7.2 节的历史评测数字与代码规模。
  - Claude 补的：手册截图缩到 1600 px、转 256 色（原每张 600–840 KB，docx 3.8 MB），`capture_manual.py` 拍完自动压缩，加体积上限测试；本文件恢复分工表待办、答辩日期改为 10-12。
- 回归：2237 通过、1 项按环境跳过（本机装了 Abaqus）；Ruff 通过。

## 下一步

1. `claude/collect-codex-pass` 的 PR 过 CI 后，经用户确认合并；之后主文件夹切回 master 并拉取。
2. **中期答辩：2026-10-12（周一），五分钟**。国庆假期（10-01 至 10-07）组员可能不在，需要一起做的事放在假期前：
   - 9-28 前：**报告第 8 节分工表由组员填好**（还是「（待填）」，答辩几乎一定会问）；定演示脚本和幻灯片大纲。
   - 9-29 至 9-30：在真窗口里验证 Esc；组员碰一次头，对齐每人讲哪部分。
   - **10-02：冻结演示版本并打 tag，之后不加功能**；录演示视频兜底。
   - 10-05：幻灯片完整初稿，线上收意见；10-06 至 10-09 每天计时排练；10-09 定稿；10-11 在答辩用的电脑上试放。
   - 演示脚本：一句话建模 → 预演确认 → 求解 → 云图 / 内力图 / 应力比 → 出报告。节点实体不现场跑（3 档要 352 s）。

## 已知问题

- **节点实体给不出 Kt**：用户模型的 6 杆节点（D219×8，节点 14）3 档网格（94.0 / 62.7 / 41.8 mm）峰值应力 290 / 245 / 219 MPa，热点应力收敛判断为「不确定」，按规则不给 Kt。更细的网格超出自研求解器规模（8 万单元 / 18 万自由度），需用 Abaqus。
- **Esc 取消拾取、关闭抽屉**只在无头测试里验证过，还没在真窗口里手动确认。
- 视口里的云图标题与色标说明是英文（"BEAM CENTERLINE INTERNAL-FORCE RESULT"、"clip p95"），界面其余部分是中文；演示前考虑是否改成中文。
- `desktop/main_window.py` 仍较长；命令、结果、设置已拆出，后续可按建模/展示流程继续分拆。
