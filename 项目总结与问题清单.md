# 空间刚架智能计算 Agent — 项目总结

> **历史快照（2026-09-05 至 09-06）**：本文保留早期问题清单，不代表当前实现或待办。
> 当前能力、回归数字和已知问题请看 `README.md` 与 `docs/PROGRESS.md`。

> **2026-09-06 续改状态（优先于下方旧清单）**
>
> - **内核数值稳定性已升级**：自由自由度刚度矩阵在分解前做对称 Jacobi 对角缩放，避免长短杆、大小截面以及平动/转动刚度跨多个数量级时，把合法复杂刚架误判成机构；结果仍回到模型原单位，并有极端刚度比解析算例锁定。
> - **后处理三条错误链已修复**：变形中心线统一由内核恢复，节点横移不再重复叠加；单单元简支梁在桌面 VTK、静态 PNG、Plotly 中都能显示真实跨中挠曲并据此自动放大；相邻杆件公共节点不再合并杆端标量，集中力处剪力跳跃也不会被等距重采样抹平。
> - 空间云图默认显示与杆件局部轴旋转无关的 **合弯矩 |M|**，合量使用从零开始的顺序色标；N/Vy/Vz/T/My/Mz 仍保留为有符号局部分量并使用关于零对称的发散色标。N-mm-MPa 模型的 PNG/Plotly 坐标标签、位移和弯矩显示换算已统一走 `units.py`。
> - 参数化窗口默认只生成几何，不再混入材料、截面、梁端释放、柱脚和梁荷载；流程按 **Geometry → Property → Initial BC → Step/Load → Solve** 展开。原来的一步建模保留在折叠的“高级快捷指派”中，生成器新增 `base="free"` 支持。
> - 建模流程已改为 **Part/Geometry → Property/Section Assignment → Initial BC → Analysis Step Load → Solve**。节点和杆件可先建成草稿，不再强制“先定截面、后定坐标”。
> - 参数化建模已支持不等跨、多层多榀、门式刚架、单榀生成后拉伸，以及用任意 `x:z` 折线和各柱柱脚坐标描述的不规则刚架；重复坐标会自动吸附，重合但未连接的节点会在求解前拦截。
> - **2026-09-08 更新：** Agent 已注册 **44 个**确定性工具，新增工作流状态、结构化变更预演与用户确认闸门；完整当前状态以 `README.md` 和 `docs/BETA_0.1_CAPABILITY_MATRIX.md` 为准。
> - 载荷与边界条件的数据结构已统一。多条同节点/同杆件的命名荷载会物理叠加，按名称编辑/删除，不再被字典键静默覆盖；Initial 支座与分析步给定位移已在界面上分开。
> - Windows 启动器已改为 ASCII/CRLF，并会检测失效虚拟环境后寻找可用解释器；`run_desktop.bat --check` 已完整通过 Python、内核、求解器、Qt、PyVista 与 OpenGL 自检。
> - Qt 无头测试不再实例化真实 VTK 交互器，避免 `processEvents()` 触发原生崩溃。2026-09-10 续改回归收集 **1258 项测试：1257 项通过、1 项按环境条件跳过**；仅有 VTK 对 NumPy 2.5 的上游弃用警告。
> - **多模态 MM2-07 已完成**：识别草稿先生成带完整哈希的结构化 diff，确认后才原子写入正式模型与追溯侧车；陈旧基线拒绝，失败不改模型/结果/历史，成功只有一个撤销步骤。保存继续使用兼容的 Domain IR JSON，图片证据单独写入并校验 `<stem>.multimodal.json`。
> - **多模态 MM2-08 已完成**：发布门禁固定绑定 30 张原创 CC0 图片、ground truth 和脱敏响应 fixture，离线评分覆盖节点、物理杆件、交点、支座、荷载、尺度和问题召回。冻结回放指标均为 1.0；这证明可复现性，不宣称在线 provider 准确率。三家在线 smoke 因未配置环境变量均明确跳过。
> - **多模态 V2 桌面闭环已补齐**：参考杆件尺度标定、问题处理、节点拖动、杆件补删、支座/命名节点荷载编辑均写回 authoritative draft；结构化 diff 后原子加载。已有模型上的重合节点必须显式复用，提交成功只占一个撤销步骤且不自动求解。
> - **真实图片验收工具链已补齐**：私有图片与合成语料隔离，授权、人工复核真值和 provider 响应齐全后才可哈希冻结并评分。当前真实集为 0 张，因此仍不宣称在线视觉准确率。
> - **桌面前端已重构为专业工程工作台**：浅色 CAD 控制台包围深色三维视口，显式加载中文字体；工具带按视角/选择/编辑/工作平面/结果上下文分成两层，左右停靠区采用稳定宽度，空项目提供三条直接入口；AI 启动钮仅在助手关闭时出现，不再遮挡模型视口。
> - 学习轨迹会保存工具实参、结果、模型变化和最终模型；只有 `verified=true` 的轨迹才应作为正确样本，失败轨迹用于学习修复过程。
> - **2026-09-06 内核升级**：截面可用 `Ay/Az` 启用 Timoshenko 剪切变形，留空则严格保持 Euler-Bernoulli；杆件 `offset_i/offset_j` 作为刚臂进入刚度、质量、几何刚度、荷载与内力恢复；新增逐级加载、迭代更新轴力的二阶弹性 P-Δ，以及带返回映射/残差收敛记录的双线性轴向材料塑性。
> - **后处理升级**：振型/失稳模态不再只连两个节点平移，而是用梁端平移和转角恢复单元内曲线；杆端释放先回算真实端转角；振型缩放只看恢复后的中心线平移，不再混用“长度”和“弧度”。变形节点增加共享节点球，刚域另画节点—梁端刚臂，避免视觉断缝。
> - **前端升级**：Property 模块新增显式“创建铰接”，杆件属性新增 i/j 端刚域偏移；Step/分析模块新增线性静力、P-Delta、双线性轴向材料非线性选择与增量数；材料/截面对话框分别提供塑性参数和 Timoshenko 开关。
> - 能力边界：P-Δ 是 **小应变二阶弹性**，不是 Abaqus 通用有限转动 `NLGEOM`；材料非线性当前只验证到 **轴向双线性塑性，弯曲保持弹性**。弯曲屈服必须引入纤维截面后才能正式宣称支持；接触、温度荷载仍未实现。
>
> 下方内容保留为历史设计记录；其中“9 个工具”“先定义属性”“完整流程未验证”“梁方向未实现”“全量测试未重跑”等条目已过时。

## 一、项目定位

**自然语言 → 结构模型 → 求解 → 自校验 → 出图，全流程可验证。**

- 代码规模：**55038 行**，**180 个 Python 文件**（2026-09-10 实测）

  | 目录 | 文件 | 行数 |
  |---|---:|---:|
  | `src/` 求解与建模内核 | 49 | 19589 |
  | `tests/` | 76 | 17971 |
  | `desktop/` 桌面端 | 36 | 14281 |
  | `tools/` | 5 | 986 |
  | `multimodal_eval/` | 4 | 819 |
  | `abaqus_bench/` | 6 | 711 |
  | `evalset/` | 4 | 681 |

- 金标准：**28 个**独立解析算例（`run_gold.bat`），真值不来自本项目的另一份实现
- 测试：全量回归见 `CHANGELOG.md` 最新条目；**写报告前请在本机跑一次全量**，
  不要引用文档里的旧数字——这一条本身就是被这份清单坑过一次才写上的
- 核心原则：**大模型只产结构，不产数值**——报告里每个数字都能溯源到某次工具调用

---

## 二、目录结构

```
agent开发/
├── src/                    核心层（39个文件，约9759行）
│   ├── frame3d.py          求解内核：3D梁单元、坐标变换、杆端释放、多工况求解
│   ├── agent.py            Agent层：系统提示、8个工具、会话状态、对话循环（134KB）
│   ├── model_io.py         Agent契约：JSON Schema + 装配 + 两级校验
│   ├── generator.py        参数化生成器：参数→节点与杆件拓扑
│   ├── plot3d.py           三维绘图：变形图、轴力图
│   ├── plot3d_interactive.py  交互式3D绘图
│   ├── sections.py         截面属性计算
│   ├── units.py            单位制
│   ├── internal_forces.py  内力计算
│   ├── modal.py            模态分析
│   ├── buckling.py         屈曲分析
│   ├── report.py           报告生成
│   ├── sketch_parser.py    多模态草图解析（手绘图片→结构模型）
│   ├── section_optimizer.py 截面优化
│   ├── silent_failures.py  静默失败检测
│   ├── capsule.py          实验胶囊存档
│   ├── mcp_server.py       MCP服务器
│   ├── abaqus_backend.py   Abaqus后端
│   ├── inp_writer.py       Abaqus INP文件写入
│   └── ...（其他辅助模块）
│
├── desktop/                桌面端（35个文件，约6498行）
│   ├── app.py              入口：QT_API设置、自检、主题、主窗口
│   ├── main_window.py      主窗口：所有面板注册、功能区、菜单、动作分派（84KB）
│   ├── viewport.py         3D视口：PyVista/VTK渲染、拾取、建模模式
│   ├── scene.py            场景管理：节点/杆件数据、最近邻搜索
│   ├── ribbon.py           功能区：按模块分页（建模/属性/载荷/分析/结果/视图/项目）
│   ├── commands.py         命令定义：57个命令（名称/标签/图标/处理函数/快捷键）
│   ├── chat_panel.py       AI对话面板：对话历史、输入框、工具调用显示
│   ├── sketch_dialog.py    2D草图对话框：连续画线、画矩形、拉伸参数
│   ├── sketch_panel.py     多模态草图识别：上传图片→AI识别→生成模型
│   ├── bc_panel.py         边界条件面板：工况管理、支座约束、节点力、杆件荷载
│   ├── properties.py       属性面板：选中对象属性显示/编辑
│   ├── model_tree.py       模型树：模型层级显示
│   ├── diagram_panel.py    内力图面板
│   ├── result_rows.py      结果表格
│   ├── section_opt_panel.py 截面优化面板
│   ├── drawers.py          抽屉与两侧窄栏：面板收进左/右/底三个抽屉，叠在视口上不挤占
│   ├── material_dialog.py  材料定义对话框
│   ├── section_dialog.py   梁截面对话框（8种截面类型）
│   ├── bc_dialog.py        边界条件对话框（6自由度勾选，5种预设）
│   ├── load_dialog.py      载荷对话框（集中力/线载荷/重力）
│   ├── theme.py            主题/颜色定义
│   ├── icons.py            图标（全部QPainter绘制，无图片资源）
│   ├── worker.py           后台线程（Runner，求解和大模型调用不阻塞界面）
│   └── doctor.py           自检脚本（启动失败时排查问题）
│
├── gui_app.py              网页端（Streamlit，74KB，功能与桌面端等价）
├── tests/                  测试（8256行，全部离线）
├── evalset/                14题评测集 + 评分器 + 成绩单
├── abaqus_bench/           与Abaqus对标：5算例 × B33/B31
├── docs/                   文档
├── examples/               示例模型、端到端脚本、离线演示
├── capsules/               实验胶囊存档
├── results/                求解结果
├── run.bat                 建环境、跑测试与离线演示
├── run_desktop.bat         桌面端启动
├── run_gui.bat             网页端启动
├── run_live.bat            真实大模型跑探针题
├── run_eval.bat            跑评测集
└── README.md               项目说明
```

---

## 三、核心模块详解

### 3.1 求解内核（src/frame3d.py，28KB）

- **单元类型**：空间梁（Euler-Bernoulli，6自由度/节点，12×12刚度矩阵）
- **坐标变换**：局部坐标系→全局坐标系
- **杆端释放**：任意杆端局部自由度，静力凝聚实现
- **多工况求解**：多工况共用一次矩阵分解，组合按线性叠加
- **内力回算**：杆端力、轴力、剪力、弯矩
- **静力平衡校核**：校验外力与内力平衡
- **奇异诊断**：定位刚体位移模态

### 3.2 Agent层（src/agent.py，134KB）

**Session类**：会话状态管理，核心方法：
- `add_nodes(coordinates)` — 添加节点
- `add_members(pairs, section, material)` — 添加杆件
- `edit_node(id, fix)` — 编辑节点约束
- `edit_member(id, section)` — 编辑杆件截面
- `add_load_case(name)` — 添加工况
- `set_nodal_load(node_id, load[6], case_name)` — 设置节点荷载
- `set_member_load(member_id, load[3], case_name)` — 设置杆件荷载
- `remove_nodes(ids)` / `remove_members(ids)` — 删除
- `generate_frame(...)` — 参数化生成规则刚架
- `generate_portal_frame(...)` — 生成门式刚架
- `validate_model()` — 两级校验
- `solve_model()` — 先校验再求解
- `modal_analysis()` — 模态分析
- `buckling_analysis()` — 屈曲分析
- `diagnose_supports()` — 定位缺失约束

**9个Agent工具**：
1. `define_materials_and_sections` — 定义材料与截面
2. `generate_frame` — 按参数展开规则刚架
3. `set_model` — 提交完整JSON（不规则结构逃生舱）
4. `set_load_cases` — 一次定义全部工况与组合
5. `validate_model` — 两级校验，返回中文错误清单
6. `solve_model` — 先校验再求解，失败附奇异诊断
7. `query_results` — 最大位移、支座反力、杆端力
8. `plot_results` — 变形图/轴力图
9. `diagnose_supports` — 定位缺失约束

### 3.3 模型IO（src/model_io.py）

- **JSON Schema**：定义模型数据结构
- **两级校验**：
  - 第一级：结构校验（字段类型、必填项、范围）
  - 第二级：语义校验（节点编号存在、杆件连接有效、约束合理性）
- **模型装配**：将零散数据组装成完整模型

### 3.4 桌面端主窗口（desktop/main_window.py，84KB）

**布局结构**：
```
┌──────────────────────────────────────────────────┐
│  菜单栏 + 功能区（建模/属性/载荷/分析/结果/...）  │
├──────────┬───────────────────────┬───┬───────────┤
│          │                       │   │           │
│  模型树  │      3D 视口区        │ ○ │  AI 助手  │
│  属性面板 │    (PyVista/VTK)      │按 │  （右侧）  │
│          │                       │钮 │           │
├──────────┴───────────────────────┴───┴───────────┤
│  💡 提示区 | 模型状态 | 求解状态                   │
└──────────────────────────────────────────────────┘
```

**动作分派机制**：
- `_run_command(cmd)` — 通过`getattr(self, cmd.handler)`动态获取方法
- 找不到处理函数时弹提示框，不静默失败
- 任何按钮失败时弹出错误对话框显示具体错误

**拾取系统**：
- `set_pick_node()` / `set_pick_member()` — 进入拾取模式
- `_on_picked(kind, ident)` — 拾取后保存选中对象
- 选中对象后可创建边界条件、载荷、截面指派、查看属性

---

## 四、完整运行逻辑

### 4.1 启动阶段

```
1. 设置 QT_API=pyside6（必须在import Qt之前）
2. 创建 QApplication
3. 应用主题（theme.STYLESHEET）
4. 创建 MainWindow
   ├─ 初始化 Session（空模型）
   ├─ 创建 Viewport（3D视口）
   ├─ 创建所有 Dock 面板：
   │   ├─ 左侧：模型树、属性（默认隐藏）
   │   ├─ 右侧：AI助手（默认显示）、手绘草图（隐藏）、边界条件（隐藏）
   │   └─ 底部：结果、内力图、截面优化（全部隐藏，tabify）
   ├─ 构建功能区（7个页签）
   ├─ 构建菜单栏
   ├─ 构建状态栏（提示区 + 状态标签）
   ├─ 创建浮动按钮（右侧中间，AI对话框左边）
   └─ 刷新视图
5. 显示窗口
6. 进入事件循环
```

### 4.2 建模阶段（三种方式）

**方式一：草图建模（人工手绘）**
```
1. 点击「建模」→「草图建模」
2. 打开 SketchDialog（850x620）
3. 用户操作：
   ├─ 连续画线模式（默认）：点击画布逐点连线
   ├─ 画矩形模式：点击两个对角自动创建4节点4边
   ├─ 实时显示：虚线预览、长度标注、宽高尺寸
   └─ 删除：点击节点/线段删除
4. 设置拉伸参数：
   ├─ 榀数（沿进深方向的榀数）
   ├─ 榀距（每榀之间的距离）
   └─ 基准面（XY/XZ/YZ）
5. 点击「生成模型」
6. 调用 Session.add_nodes() / add_members() 创建3D模型
7. 刷新3D视图
```

**方式二：规则框架（参数化生成）**
```
1. 点击「建模」→「规则框架」
2. 打开参数对话框
3. 输入：跨数、跨度、层数、层高、榀数、榀距、柱底约束
4. 调用 Session.generate_frame() 生成模型
5. 刷新3D视图
```

**方式三：多模态识别（AI识别手绘草图）**
```
1. 点击「建模」→「草图识别」
2. 打开 SketchPanel（右侧dock）
3. 配置多模态模型：
   ├─ 提供商（openai/anthropic/deepseek）
   ├─ 模型名（gpt-4o / claude-3-5-sonnet / deepseek-vl）
   └─ API密钥（不填读环境变量）
4. 选择手绘草图图片（PNG/JPG/JPEG/WebP）
5. 点击「识别」
6. 多模态LLM分析图片，输出节点/杆件/支座/荷载的JSON
7. sketch_parser.py 解析并校验
8. 调用 Session 方法创建模型
9. 刷新3D视图
```

### 4.3 属性阶段

```
1. 点击「属性」→「创建材料」
   ├─ 打开 MaterialDialog
   ├─ 输入：名称、弹性模量E、泊松比ν、密度
   └─ 保存到 Session.materials

2. 点击「属性」→「创建截面」
   ├─ 打开 SectionDialog
   ├─ 选择截面类型（8种：I型钢/矩形管/圆管/矩形/圆钢/槽钢/等边角钢/T型钢）
   ├─ 输入尺寸参数
   ├─ 自动计算：A（面积）、Iy/Iz（惯性矩）、J（扭转常数）
   ├─ 实时预览截面形状
   └─ 保存到 Session.sections

3. 截面指派：
   ├─ 点击「建模」→「选择杆件」
   ├─ 在3D视口中点击杆件选中
   ├─ 点击「属性」→「截面指派」
   ├─ 选择材料和截面
   └─ 调用 Session.edit_member() 保存
```

### 4.4 载荷阶段

```
1. 创建边界条件：
   ├─ 点击「建模」→「选择节点」
   ├─ 在3D视口中点击柱底节点选中
   ├─ 点击「载荷」→「创建边界条件」
   ├─ 打开 BCDialog
   ├─ 勾选6个自由度（U1/U2/U3/UR1/UR2/UR3）
   ├─ 或选择预设（固定端/铰接/X向滚动/Z向滚动/自由）
   └─ 调用 Session.edit_node() 保存约束

2. 创建载荷：
   ├─ 选择节点（集中力）或杆件（线载荷）
   ├─ 点击「载荷」→「创建载荷」
   ├─ 打开 LoadDialog
   ├─ 选择载荷类型（集中力/线载荷/重力）
   ├─ 输入分量（CF1/CF2/CF3/CM1/CM2/CM3 或 P1/P2/P3）
   └─ 调用 Session.set_nodal_load() / set_member_load() 保存

3. 重力荷载：
   ├─ 点击「载荷」→「重力」
   ├─ 自动根据材料密度和截面面积计算
   └─ 施加到所有杆件
```

### 4.5 分析阶段

```
1. 模型检查（AI辅助校验）：
   ├─ 点击「分析」→「模型检查」
   ├─ 检查9项问题：
   │   ├─ 模型为空
   │   ├─ 没有杆件
   │   ├─ 没有支座
   │   ├─ 没有荷载
   │   ├─ 孤立节点
   │   ├─ 重复杆件
   │   ├─ 零长度杆件
   │   ├─ 柱底无约束
   │   └─ 缺少材料截面
   └─ 给出修正建议

2. 求解：
   ├─ 点击「分析」→「求解」
   ├─ 先调用 validate_model() 两级校验
   ├─ 校验不通过：返回中文错误清单，不求解
   ├─ 校验通过：调用 frame3d.solve() 求解
   │   ├─ 组装全局刚度矩阵
   │   ├─ 施加约束（边界条件）
   │   ├─ 求解线性方程组
   │   ├─ 回算杆件内力
   │   └─ 静力平衡校核
   ├─ 失败时：附奇异诊断（定位刚体位移模态）
   └─ 保存结果到 Session.result
```

### 4.6 结果阶段

```
1. 变形图：
   ├─ 点击「结果」→「变形」
   ├─ 视口显示变形后的模型
   ├─ 可调节变形放大系数

2. 云图：
   ├─ 点击「结果」→「云图」
   ├─ 选择分量（位移/应力）
   ├─ 视口显示彩色云图
   └─ 显示图例

3. 内力图：
   ├─ 点击「结果」→「内力分量」
   ├─ 选择杆件
   ├─ 底部显示内力图（轴力/剪力/弯矩）

4. 结果表格：
   ├─ 底部「结果」面板
   ├─ 显示：最大位移、支座反力、杆端力
   └─ 带坐标与符号约定
```

### 4.7 AI辅助流程

```
1. 用户点击右侧浮动按钮，展开AI助手对话框
2. 用户输入自然语言描述，如："建一个三跨两层的框架，柱底固接"
3. Agent层处理：
   ├─ 大模型理解用户意图
   ├─ 决定调用哪个工具（如 generate_frame）
   ├─ 生成工具调用参数（如 spans=3, storeys=2, ...）
   ├─ 调用工具执行
   ├─ 工具返回结果（节点/杆件编号）
   ├─ 大模型根据结果决定下一步（如施加荷载、求解）
   └─ 循环直到完成用户需求

4. 自修复闭环：
   ├─ 调用 validate_model() 校验
   ├─ 校验不通过：返回中文错误清单给大模型
   ├─ 大模型根据错误修改模型
   └─ 重新校验，直到通过

5. 核心约束：大模型只产结构（拓扑/约束类型），不产数值（位移/内力）
   所有数值必须来自工具返回（求解器计算）
```

---

## 五、核心设计原则

| 原则 | 说明 |
|------|------|
| **大模型只产结构，不产数值** | 大模型决定"三跨两层、柱底固接"，位移内力反力必须来自工具返回 |
| **拓扑由代码展开** | 多层多跨走参数化生成器，不逐个列节点坐标；不规则结构才走 set_model 逃生舱 |
| **算前守门** | set_model 与 solve_model 都先跑两级校验，不通过就不算，把错误清单回给模型自修复 |
| **图给人看，数给模型看** | plot_results 只返回文件路径和数值摘要，不返回图像；不让多模态模型看云图下结论 |
| **不静默失败** | 任何按钮失败时弹出错误对话框；求解失败时附奇异诊断；启动失败时跑自检 |

---

## 六、验证体系（三层）

1. **第一层 解析解**：悬臂挠度 PL³/3EI、扭转角 TL/GJ、简支跨中 5wL⁴/384EI 等，全部机器精度命中
2. **第二层 数值对标**：与已知数值解对标
3. **第三层 Abaqus对标**：5个算例 × B33/B31单元，结果一致

---

## 七、五个入口脚本

| 脚本 | 作用 | 联网 |
|------|------|------|
| `run.bat` | 建环境、跑测试与离线演示 | 不用 |
| `run_desktop.bat` | 桌面端（PySide6 + PyVista，CAE式界面） | 首次装依赖要 |
| `run_gui.bat` | 网页端（Streamlit，功能与桌面端等价） | 不用 |
| `run_live.bat` | 拿真实大模型跑5道探针题 | 要，密钥放 deepseek.key |
| `run_eval.bat` | 跑14题评测集，出成绩单 | 要 |

---

## 八、当前能力与限制

**支持**：
- 单元：空间梁（Euler-Bernoulli，6自由度/节点）
- 材料：各向同性线弹性
- 约束：六个方向逐个开关
- 释放：任意杆端局部自由度（静力凝聚）
- 荷载：节点力/力矩、全局坐标均布杆间荷载、重力
- 工况：多工况共用一次矩阵分解，组合线性叠加
- 生成：规则多层多跨多开间空间刚架、门式刚架
- 自校验：模型合法性、静力平衡、奇异模态定位、位移量级
- 出图：变形图、轴力图、云图、内力图
- 分析：静力、模态、屈曲
- 优化：截面优化
- 多模态：手绘草图图片识别

**还没做**：
- 剪切变形（Timoshenko梁）
- 刚域偏移
- 温度荷载
- 二阶效应（P-Δ）
- 自动计算书
- 非线性分析

---

# 当前存在的问题

## 一、已修复但未充分验证的问题

### 1. 拾取按钮缺失（已修复）
- **问题**：选择节点/选择杆件按钮不在功能区，用户无法选中对象，导致后续操作（边界条件/载荷/截面指派）全部无法进行
- **修复**：已添加到「建模」页签「选择」组，作为大按钮
- **状态**：代码已修复，**尚未端到端验证完整操作链**（拾取节点→创建边界条件→求解→看结果）

### 2. 浮动按钮被遮挡（已修复）
- **问题**：浮动按钮在所有dock widget之前创建，z-order最低，被所有面板遮挡，用户看不见
- **修复**：按钮移到所有组件创建之后，确保在最上层；位置放在AI对话框左边，不会被遮挡
- **状态**：代码已修复，**尚未验证是否正常显示和点击**

### 3. 绘制错误（已修复）
- **问题**：`QBackingStore::endPaint() called with active painter` 错误
- **原因**：floating_button.py中drawPolygon传入6个整数而非QPolygon对象；paintEvent没有异常保护
- **修复**：改用QPolygon对象；paintEvent添加try/except/finally保护
- **状态**：代码已修复

### 4. 启动崩溃（已修复）
- **问题**：`AttributeError: module 'desktop.theme' has no attribute 'GRID'/'GOOD'`
- **原因**：新建面板用了Streamlit端颜色名，desktop/theme.py实际颜色名不同
- **修复**：全部替换为正确的颜色名
- **状态**：代码已修复，导入正常

## 二、尚未解决的问题

### 1. 人工建模完整流程未验证
- **问题**：草图建模→生成3D模型→选择节点→创建边界条件→选择杆件→创建载荷→求解→看结果，这条完整链路**尚未端到端验证**
- **风险**：可能存在隐藏bug，用户操作到某一步会报错
- **优先级**：**高**

### 2. 草图生成模型可能报错
- **问题**：用户多次反馈"草图生成模型还在报错"
- **现状**：已添加异常捕获和错误提示，但**根本原因未定位**
- **可能原因**：
  - 草图数据格式与Session.add_nodes/add_members期望的格式不匹配
  - 节点编号/杆件编号逻辑有问题
  - 空模型初始化逻辑有问题
- **优先级**：**高**

### 3. 出图问题未明确
- **问题**：用户说"出图这些也有很大问题"，但**未具体说明是什么问题**
- **可能问题**：
  - 内力图显示不正确
  - 变形图/云图渲染有问题
  - 标注/图例有问题
  - 图片导出有问题
- **优先级**：**中**（需用户明确具体现象）

### 4. AI辅助人工建模形态未完全实现
- **问题**：用户要求"AI可调度工具"，但当前AI助手主要是自然语言建模，**AI作为工具被人工调用的形态未实现**
- **现状**：已实现模型检查（AI辅助校验），但AI自动补全支座/荷载、AI优化模型等功能未实现
- **优先级**：**中**

### 5. 全量测试未重新运行
- **问题**：最近大量修改（功能区重组、对话框创建、拾取修复、浮动按钮修复等）后，**全量pytest套件尚未重新运行**
- **风险**：修改可能引入回归bug
- **优先级**：**高**

### 6. 界面布局用户多次不满
- **问题**：用户多次表示"界面布局乱""很多功能都是摆设""一个都没实现"
- **可能原因**：
  - 功能区按钮位置不合理，用户找不到
  - 面板默认隐藏，用户不知道怎么打开
  - 操作流程不直观，用户不知道下一步该做什么
  - 提示区信息不够明确
- **优先级**：**高**

### 7. 8个prefill命令仍保留
- **问题**：node/member/remove/bent/extrude/sets/sweep/compare这8个命令仍在commands.py中保留定义，handler为"prefill"
- **用户要求**：明确反对任何prefill行为
- **现状**：已从功能区和菜单栏移除，但代码中仍保留定义
- **优先级**：**低**（不影响用户使用，但代码不干净）

### 8. 部分命令未在功能区使用
- **问题**：curve（内力图）和deflection（最大挠度）命令未在功能区使用
- **优先级**：**低**

### 9. 草图对话框功能不完整
- **问题**：Abaqus式草图环境的部分功能尚未实现
- **未实现功能**：
  - 尺寸标注（Create Dimension）
  - 约束（相切、平行、垂直等）
  - 修剪/延伸
  - 偏移
  - 阵列
- **优先级**：**中**

### 10. Property模块功能不完整
- **问题**：梁方向（Beam Orientation）尚未实现
- **影响**：对于非对称截面，梁方向会影响内力结果
- **优先级**：**中**

### 11. 多模态真实图片准确率尚未验证
- **现状**：离线合成夹具、V2 桌面闭环和真实集验收工具均已验证；尚未提供获授权的真实工程图，也未配置在线 provider 凭据
- **风险**：不能把 1.0 的 fixture 回放分数当作真实视觉模型准确率
- **优先级**：**中**

### 12. 界面美化未完成
- **问题**：用户多次要求"往Abaqus靠拢""美化界面"，但当前界面与Abaqus仍有较大差距
- **差距**：
  - 功能区样式不够专业
  - 3D视口缺少坐标系指示器、视图立方体
  - 状态栏信息不够丰富
  - 图标风格不统一
- **优先级**：**中**

## 三、问题优先级汇总

| 优先级 | 问题 | 状态 |
|--------|------|------|
| **高** | 人工建模完整流程未验证 | 待验证 |
| **高** | 草图生成模型可能报错 | 未定位根本原因 |
| **高** | 全量测试未重新运行 | 待运行 |
| **高** | 界面布局用户多次不满 | 持续改进中 |
| **中** | 出图问题未明确 | 需用户明确 |
| **中** | AI辅助人工建模形态未完全实现 | 待设计 |
| **中** | 草图对话框功能不完整 | 待实现 |
| **中** | Property模块梁方向未实现 | 待实现 |
| **中** | 多模态真实图片准确率尚未验证 | 工具链就绪，等待真实样本与 provider 凭据 |
| **中** | 界面美化未完成 | 持续改进中 |
| **低** | 8个prefill命令仍保留 | 待清理 |
| **低** | 部分命令未在功能区使用 | 待添加 |

---

## 四、建议的下一步行动

1. **立即运行全量测试**，确认最近修改没有引入回归bug
2. **端到端验证人工建模完整流程**，定位并修复草图生成模型的报错
3. **明确出图问题的具体现象**，针对性修复
4. **持续改进界面布局**，参考Abaqus优化操作流程
5. **实现AI辅助人工建模的具体形态**（AI自动补全支座/荷载、AI优化模型等）

---

*文档生成时间：2026-09-05*
*项目版本：桌面端 CAE 式界面改造版*
