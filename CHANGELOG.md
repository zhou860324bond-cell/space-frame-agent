# 变更日志

## 应力比图 — 2026-09-25

- 新增显示模式「应力比」（快捷栏，Ctrl+6）：每根物理构件取强度、折算应力（GB 50017）、稳定（有规范法 φ 用规范法，否则欧拉）三者最大值为利用率，按 <0.5 / 0.5–0.7 / 0.7–0.9 / 0.9–1.0 / >1.0 五档着色，超过 1.0 为红；判不了的构件（欧拉不适用的粗短柱）画灰色，不冒充合格；利用率 ≥0.9 的构件标数值。
- 验算放后台跑，同一次求解只算一次；从「强度验算」按钮算过的结果直接复用，逐杆表同时填进结果页。材料缺许用应力、截面缺 cy/cz 时说明去哪补，并退回模型显示。
- 补上界面英文：应力比、平滑彩虹（默认）、连续。
- 全量回归共收集 2213 项：2212 项通过、1 项按环境跳过。

## 前端交互与流畅度 — 2026-09-24

- 修复：出图偶发 "Axis limits cannot be NaN or Inf"（约每 15 次一次）的第二条路径——matplotlib 每加一组线/面都按数据自动重算范围，撞上非规格化浮点坐标（实测 1.24e-311）时边距溢出成 inf；画布改为关闭自动定范围，范围只由 _equalize 按节点坐标设定。修后连跑 300 次零失败。
- 修复：点了「创建载荷」等需要拾取的命令后改做别的，之后点中对象仍会弹出先前命令的对话框；现在执行任何其他命令即放弃待拾取。
- 修复：切换英文时抽屉页签、单页抽屉标题与两侧窄栏不翻译（语言切换仍按旧停靠窗处理）。
- 修复：焦点在抽屉里按 Alt+F4 会绕过抽屉的关闭流程，窗藏了但状态仍是"开着"，窄栏按钮常亮、主窗一挪抽屉又冒出来。
- 新增三维「内力图」显示模式（快捷栏「内力图」，Ctrl+5）：每根杆旁画出内力沿杆的分布，高度即数值、全结构同一比例尺，弯矩画在受拉侧，正负分画两侧并按蓝—灰—红着色；剪力、轴力这类沿杆不变的量画成矩形，大小一眼可读。原来底部抽屉的单杆曲线改名「单杆内力图」。
- 云图杆件改为按屏幕像素宽度画的带光照线管（原按模型尺寸建管，半径是普通显示的 4.5 倍，一切到云图杆件就胖一大圈）；粗细与模型大小、镜头远近无关，也省掉了管网格生成。
- 云图默认改为沿杆连续着色 + 平滑彩虹（turbo）：原 12 级分级把合弯矩切成一圈圈色环、整图像彩色条纹；分级仍可在「分级」里选。连续模式不再压橙色量程外补丁；色标与说明文字随抽屉让位，不再被结果抽屉盖住或被模式提示压住。

- 面板从九个自由浮窗改为左/右/底三个抽屉：锚定在视口内、叠在视口上不改变视口尺寸，永远不压住功能区与快捷栏；视口两侧加固定宽度的窄栏作入口。
- AI 助手改为右侧窄栏「AI 助手」一键召唤/收起。原浮动按钮画在 VTK 原生窗口上，在 Windows 下会被盖住而看不见。
- 抽屉开合时相机自动把模型移进未遮挡区域并按比例缩放，打开结果表不再挡住结构；视口的模式提示与坐标轴同步让位。
- 「创建载荷/边界条件/节点实体」不再因未选中而置灰：点命令后进入拾取，点中对象自动继续；Esc 取消。
- 求解后自动填好「求解总览」（各工况最大位移、单元内挠度、平衡），但不自动弹出；看结果时点选不再弹出属性抽屉。
- 流畅度（1330 杆满布荷载框架实测）：切到模型显示 40 s→1.2 s，求解+刷新 18 s→3 s，变形图 3.4 s→0.5 s，云图 4.4 s→1.4 s（再次切换 0.8 s），点选 0.2–0.4 s→0.02–0.08 s。来源：荷载箭头改为模板实例化、折线与云图切段向量化、每帧只渲染一次、预览帧与校验按内容缓存、杆件挠度/内力按求解缓存、重合节点检查与编译去掉 O(N²) 循环、条件数估计不再显式求逆。
- 全量回归共收集 2196 项：2195 项通过、1 项按环境跳过（含合入的 PR #17、#18）。

## 节点局部实体 — 2026-09-10

- 新增自研 `native-solid`：C3D10 形函数与四点积分、三维弹性本构、分块稀疏组装、约束消元、六分量切割面等效载荷、积分点/节点应力和 VTU/PNG 云图；Gmsh仅负责网格。
- native 新增相贯线局部加密、真实外表面识别、0.4t/1.0t 热点路径及三档稳定性门禁；未收敛的试算值保留供诊断，但不发布为正式 Kt。
- 新增圆钢/圆管节点 Abaqus C3D10 局部实体子模型：整体梁杆端六分量映射、三档网格、ODB 应力提取、IIW 热点应力与奇异峰值判别。
- 桌面端新增“分析 → 节点实体”，选中已求解节点后在后台运行，结果面板展示网格收敛表和云图路径。
- 本机 Abaqus 6.14-1 三档全链路冒烟验收通过；Mises 云图按 P99 裁剪，保留峰值数据同时提高分布可读性。
- 全量回归共收集 1353 项：1352 项通过、1 项按环境跳过。

## Agent 响应性能 — 2026-09-10

- 为“下一步、检查模型、重新求解、查看最大位移、查看反力”等明确短命令增加确定性本地快速路径，不再产生无意义的模型网络往返。
- 多轮历史增加 24,000 字符预算，并保持工具调用与工具结果按完整回合配对。
- 超长工具回包只在 LLM 上下文中折叠，Session、结果数据库和界面仍保留完整结果。
- 桌面对话状态栏显示总耗时、模型等待、工具耗时和模型往返次数，便于继续定位真实瓶颈。
- 全量回归共收集 1252 项：1251 项通过、1 项按环境跳过。

## Beta 0.1 候选版 — 2026-09-08

### 主链路

- 引入版本化 Domain IR v1，并保留无版本旧模型迁移入口。
- 建立 `PhysicalMember → AnalysisElement` 编译与双向映射。
- 支持杆中集中荷载、显式内节点触发的自动剖分，以及释放和分布荷载映射。
- 线性静力结果写入统一 Result DB；查询、绘图和报告读取同一结果合同。
- 提供桌面分析网格开关和 Agent `preview_analysis_mesh` 只读预览。

### Agent 与交互

- Agent 工具增至 44 个，新增确定性 `empty/draft/ready/solved` 工作流状态。
- 新增隔离式 `preview_change` 结构化 diff 与 `apply_preview` 确认应用。
- Agent 删除已有节点或杆件时由代码强制预演；同轮自我确认和陈旧预演均会拒绝。
- 手工建模支持工作平面偏移、网格捕捉和精确坐标建点；重复坐标只复用节点。
- 多模态图片确认链路作为实验性能力保留，不进入 Beta 0.1 正式主链路。

### 验证

- 冻结 12 个独立解析金标准算例。
- 保留 Abaqus B33/B31 对标、静默失败检测与实验胶囊。
- 新增工作流状态、变更预演、用户确认与防同轮自批准测试。
- 候选回归共收集 1049 项：1048 项通过、1 项按环境跳过；12 个金标准全部通过。

### 已知限制

- 模态仍使用原结果对象；P-Δ 和轴向双线性结果入库仅为实验性适配。
- 不自动识别任意空间交点或属性突变；集中荷载与刚域偏移组合明确拒绝。
- 梁线结果着色不是连续体应力云图，不能替代规范校核、工程审图或商业软件认证。
- 最新留存的真实模型评测为 13/14；本轮 Agent 状态机尚未重新做付费在线评测。
- Git 提交、候选标签和远端回滚点按用户要求暂不创建；当前回滚依赖应用内撤销和文件备份。
