# 变更日志

## v0.2.0-rc1 稳定化 — 2026-09-14

- 将当前变化按求解器与对标、Agent、前端交互、云图与三维显示、文档与打包五个
  责任域唯一归类，并冻结候选变更清单。
- 版本口径统一为 v0.2.0-rc1；当前测试清单为 1728 项，候选发布前只执行一次全量
  回归，日常改动改用 Agent、前端、求解器、文档四组增量入口。
- 当前候选四组增量并集为 718 项通过、2 项按环境跳过；求解器组246项包含28项金
  标准。当前自研解已重新对比既有 B33/B31 `.dat`，Abaqus 实机证据沿用2026-09-12
  冻结报告，本轮尚未重新启动外部求解器。

## 节点实体跨重启续跑 — 2026-09-14

- 双后端任务可从磁盘恢复已完成阶段；缓存同时绑定完整模型、由整体梁解提取的节点
  六分量输入和三档网格计划，不再只凭节点号与工况名复用。
- 只有全部候选杆臂通过热点收敛门禁，且 native VTU 或 Abaqus ODB 主结果文件存在、
  非空时才跳过重算；旧格式、损坏摘要和不完整结果均自动忽略。
- 节点实体结果页和双后端收敛页新增同页溯源证据带，直接展示计算/恢复来源、生成
  时间、模型与节点输入短指纹、三档提交网格和主结果文件完整性；完整 SHA-256 与
  文件路径保留在悬停说明中，旧版或损坏摘要明确降级而不阻断结果浏览。
- 每次 native/Abaqus 实体计算直接写入独立运行目录，成功后才原子更新 latest 摘要；
  新运行失败不会覆盖上一份可信结果，也无需复制大型 ODB 才能保留历史。
- 溯源带新增双版本选择器，在原结果表比较模型/节点输入指纹、提交网格、控制热点
  应力、正式 Kt 和主文件状态；输入不同时明确禁止归因于求解器或网格。
- 同一结果页新增历史空间管理：逐次统计 ODB/VTU 运行目录、文件数和总占用，只允许
  选择旧的独立运行目录并在二次确认后移入 Windows 回收站；每个后端的 latest、当前
  对比版本 A/B、旧版平铺结果、越界路径和不可核验目录均由底层拒绝清理。
- 建模、求解或后处理未形成有效摘要的运行目录不再成为不可见的磁盘残留：管理表将其
  标为不完整并推断中断阶段。最近 24 小时仍有写入的目录视为可能在运行，只展示不清理；
  超过保护期后才进入回收站候选，执行前仍重新核对 mtime、latest 和目录身份。
- “分析→实体结果中心”现在直接扫描全部受管理的 native/Abaqus 节点与工况，不依赖
  当前选择或成功结果；首次计算即失败、尚未形成 latest 的旧残留也可在 24 小时保护期
  后安全移入回收站，存在但损坏的 latest 仍按不可核验拒绝清理。

## B31 对标闭环 — 2026-09-12

- Abaqus B31 导出现在把自研截面的 `Ay/Az` 显式映射为 `K13=G·Ay`、
  `K23=G·Az`，并关闭默认细长梁补偿；Abaqus 后端新增 `auto`，自动按截面理论选择
  B31/B33，混用两类截面时明确拒绝猜测。
- 新增同参数七档报告 `benchmark_B31_matched.md`：64× 时门架和空间框架全场位移
  误差分别为 0.00277% 和 0.0173%，剩余误差随细化约四倍下降。
- 用 Abaqus 6.14 对门架和空间框架完成 B31 的 1×/2×/4×/8×/16×/32×/64× 杆件细化，确认粗网格低阶插值误差与 Timoshenko 剪切柔度会相互抵消；高密度极限趋近 4.61% 和 4.54%，不再把 8× 结果误写成已收敛。
- 新增可复现脚本 `abaqus_bench/b31_refinement.py` 与冻结报告 `benchmark_B31_refinement.md`；每档先验证细分前后自研侧原节点解，64× 的最大实测漂移不超过 `4e-12 m`。
- 对标报告统一使用“最大节点位移”，避免把未采样的连续梁跨内极值误称为全结构最大位移。

## 节点局部实体 — 2026-09-10

- 新增自研 `native-solid`：C3D10 形函数与四点积分、三维弹性本构、分块稀疏组装、约束消元、六分量切割面等效载荷、积分点/节点应力和 VTU/PNG 云图；Gmsh仅负责网格。
- native 新增相贯线局部加密、真实外表面识别、0.4t/1.0t 热点路径及三档稳定性门禁；未收敛的试算值保留供诊断，但不发布为正式 Kt。
- 新增圆钢/圆管节点 Abaqus C3D10 局部实体子模型：整体梁杆端六分量映射、三档网格、ODB 应力提取、IIW 热点应力与奇异峰值判别。
- 桌面端新增“分析 → 节点实体”，选中已求解节点后在后台运行，结果面板展示网格收敛表和云图路径。
- 本机 Abaqus 6.14-1 三档全链路冒烟验收通过；Mises 云图按 P99 裁剪，保留峰值数据同时提高分布可读性。
- 全量回归共收集 1353 项：1352 项通过、1 项按环境跳过。

## Agent 响应性能 — 2026-09-10

- 为“下一步、检查模型、重新求解、查看最大位移、查看反力”等明确短命令增加确定性本地快速路径，不再产生无意义的模型网络往返。
- 多轮历史增加 24,000 字符预算，并保持工具调用与工具结果按完整回合配对。
- 超长工具回包只在 LLM 上下文中折叠，Session、结果数据库和界面仍保留完整结果。
- 桌面对话状态栏显示总耗时、模型等待、工具耗时和模型往返次数，便于继续定位真实瓶颈。
- 全量回归共收集 1252 项：1251 项通过、1 项按环境跳过。

## Beta 0.1 候选版 — 2026-09-08

### 主链路

- 引入版本化 Domain IR v1，并保留无版本旧模型迁移入口。
- 建立 `PhysicalMember → AnalysisElement` 编译与双向映射。
- 支持杆中集中荷载、显式内节点触发的自动剖分，以及释放和分布荷载映射。
- 线性静力结果写入统一 Result DB；查询、绘图和报告读取同一结果合同。
- 提供桌面分析网格开关和 Agent `preview_analysis_mesh` 只读预览。

### Agent 与交互

- Agent 工具增至 44 个，新增确定性 `empty/draft/ready/solved` 工作流状态。
- 新增隔离式 `preview_change` 结构化 diff 与 `apply_preview` 确认应用。
- Agent 删除已有节点或杆件时由代码强制预演；同轮自我确认和陈旧预演均会拒绝。
- 手工建模支持工作平面偏移、网格捕捉和精确坐标建点；重复坐标只复用节点。
- 多模态图片确认链路作为实验性能力保留，不进入 Beta 0.1 正式主链路。

### 验证

- 冻结 12 个独立解析金标准算例。
- 保留 Abaqus B33/B31 对标、静默失败检测与实验胶囊。
- 新增工作流状态、变更预演、用户确认与防同轮自批准测试。
- 候选回归共收集 1049 项：1048 项通过、1 项按环境跳过；12 个金标准全部通过。

### 已知限制

- 模态仍使用原结果对象；P-Δ 和轴向双线性结果入库仅为实验性适配。
- 不自动识别任意空间交点或属性突变；集中荷载与刚域偏移组合明确拒绝。
- 梁线结果着色不是连续体应力云图，不能替代规范校核、工程审图或商业软件认证。
- 最新留存的真实模型评测为 13/14；本轮 Agent 状态机尚未重新做付费在线评测。
- Git 提交、候选标签和远端回滚点按用户要求暂不创建；当前回滚依赖应用内撤销和文件备份。
